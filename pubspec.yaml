name: transit_ai
description: TransitAI Mobile App
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.0
  qr_flutter: ^10.0.0
  url_launcher: ^6.1.0
  mobile_scanner: ^3.4.0
  geolocator: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
